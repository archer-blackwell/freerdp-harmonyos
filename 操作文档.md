# FreeRDP HarmonyOS 操作文档

> 适用对象：当前工程 `freerdp-harmonyos`（FreeRDP 3.31.1-dev0 鸿蒙移植）。
> 状态基线：2026-09-10 第八轮真机（2UCUT24C03006381）回归通过。
> 本文描述代码的**真实行为**，目录/命令/常量/日志均来自源码核对；代码变更后请同步更新本文。

---

## 1. 项目简介

- 基于 FreeRDP 3.31.1 的 HarmonyOS RDP 远程桌面客户端（Stage 模型，API 12 / 5.0.0(12)）
- 应用标识：bundleName `com.example.twgapplication`，versionName 1.0.0（AppScope/app.json5）
- 单模块工程：`entry`（entry HAP），deviceTypes：phone / tablet
- 核心能力：
  - RDP 连接与渲染：XComponent(SURFACE) + native 直渲
  - 远端光标同步（类型映射 + 自绘叠加层）
  - 中文输入法桥接（拼音组合跟踪、选字上屏、退格删远端）
  - 触控手势：单击 / 双击 / 长按拖动 / 滑动滚轮（纵横）/ 双指右键 / 捏合缩放 / 双指平移
  - 横竖屏切换 + 桌面分辨率跟随
  - 特殊键面板（F1–F12 / 修饰键 / 导航键）
  - 剪贴板、音频（OpenSLES）、后台保活、自动重连、心跳、TCP keepalive

---

## 2. 环境要求

| 项 | 说明 |
|---|---|
| DevEco Studio | 含 HarmonyOS SDK/NDK（本机：`E:\HarmonyOs\DevelopTools\huawei\devecostudio\application\DevEco Studio`） |
| hvigor | 随 DevEco 安装；构建直连 hvigorw.bat（见 4.1） |
| hdc | 随 DevEco 安装；真机需开启开发者模式 + USB 调试 |
| 设备 | 真机示例 `2UCUT24C03006381`（命令中以 `-t <设备号>` 指定） |

> ⚠️ **Node 版本坑**：DevEco 自带 Node v18.20.1 会遮蔽 nvm 安装的 Node 25，导致 `build_project`、`devecocli` 等工具不可用。构建一律使用 4.1 的直连命令。

---

## 3. 项目结构

```
AppScope/app.json5                          应用级配置（bundleName / 版本 / 图标）
build-profile.json5                         工程 / 产品 / 签名（debug 材料在 C:\Users\USER2022\.ohos\config\）
entry/src/main/module.json5                 权限 4 项 + EntryAbility + backgroundModes
entry/src/main/ets/
  entryability/EntryAbility.ets             入口：初始化 RdpSessionManager/NetworkManager/AudioFocusManager，加载 pages/Index
  entrybackupability/EntryBackupAbility.ets 备份扩展
  pages/Index.ets                           首页：书签列表 + 快速连接
  pages/SessionPage.ets                     会话页（核心，约 1938 行）
  pages/BookmarkPage.ets                    书签编辑
  pages/SettingsPage.ets                    设置
  pages/AboutPage.ets                       关于
  services/LibFreeRDP.ets                   FreeRDP NAPI 封装（全静态类 + 常量 + 事件接口）
  services/RdpSessionManager.ets            会话管理（EntryAbility 初始化链）
  services/RdpBackgroundService.ets         后台保活（continuous task + 通知）
  services/NetworkManager.ets               网络管理（SessionInfo）
  services/AudioFocusManager.ets            音频焦点
  model/SessionState.ets                    ConnectionState / SessionState / GlobalSessionManager
  model/BookmarkBase.ets                    书签模型（ManualBookmark / QuickConnectBookmark）
  components/VirtualKeyboard.ets            特殊键面板
  components/SessionView.ets                ⚠ 遗留，未被引用
  components/TouchPointerView.ets           ⚠ 遗留，未被引用
  utils/KeyboardMapper.ets                  ⚠ 遗留，未被引用
  utils/ClipboardManager.ets                ⚠ 遗留，未被引用
entry/src/main/cpp/                         NAPI wrapper：harmonyos_freerdp.cpp / harmonyos_napi.cpp /
                                            harmonyos_event.c / harmonyos_cliprdr.c / harmonyos_jni_callback.c /
                                            harmonyos_jni_utils.c / freerdp_client_compat.c + CMakeLists.txt
entry/libs/arm64-v8a/                       预编译库：libfreerdp3 / libfreerdp-client3 / libwinpr3（各 .so/.so.3/.so.3.31.1）
                                            + libfreerdp_harmonyos.so（wrapper 构建产物）
entry/libs/include/freerdp3/ + winpr3/      编译期头文件
entry/src/main/resources/
  base/element/                             string.json / color.json / float.json
  base/media/                               应用图标（startIcon / foreground / background / layered_image）
  base/profile/                             main_pages.json / main.json / backup_config.json
  rawfile/                                  portrait_orientation.svg / landscape_screen.svg（会话页方向按钮）
scripts/rebuild-ohos-*.ps1、download-libs.ps1   原生库重建脚本
native-src/ native-build/ native-install/   原生依赖源码包与交叉编译产物
REBUILD_NATIVE_OHOS.md                      原生重建文档
```

> 原生链说明：CMake 编译 wrapper（链接预编译 FreeRDP 三件套）；OpenSSL 已静态链入 FreeRDP 库，FFmpeg 构建期禁用，无额外动态依赖。

---

## 4. 构建与部署

### 4.1 构建（PowerShell）

```powershell
& "E:\HarmonyOs\DevelopTools\huawei\devecostudio\application\DevEco Studio\tools\hvigor\bin\hvigorw.bat" --mode module -p module=entry@default -p product=default -p buildMode=debug assembleHap --analyze=false --parallel --incremental --daemon=false
```

- release 构建：把 `-p buildMode=debug` 改为 `-p buildMode=release`
- 产物：`entry\build\default\outputs\default\entry-default-signed.hap`

### 4.2 安装到设备

```powershell
hdc list targets
hdc -t 2UCUT24C03006381 install -r entry\build\default\outputs\default\entry-default-signed.hap
```

### 4.3 签名

- debug 签名材料已配置于根 `build-profile.json5`（`C:\Users\USER2022\.ohos\config\default_freerdp-harmonyos_*.cer/.p7b/.p12`）
- 签名失效或更换机器时，参考根目录《签名配置指南.md》《获取本地签名配置.md》等系列文档

---

## 5. 页面与连接流程

### 5.1 首页 Index

- 书签列表：Preferences 持久化（store `bookmarks`，键 `bookmarks`，JSON 序列化）
- 快速连接：主机 / 端口（默认 3389）/ 用户名 / 密码
- 原生库自检：加载失败在页面显示 `LibFreeRDP.getLoadError()`；成功打印 `getVersion()`
- ⚠ 首次启动无书签时会写入默认测试书签（Lenovo / 172.16.42.207:3389，见 `buildDefaultTestBookmarks()`）——**对外发布前必须移除**（含内网地址与测试凭据）

### 5.2 书签编辑 BookmarkPage

手动书签属性：主机 / 端口 / 账号 / 分辨率预设 / 安全协议 / 声音选项等。

### 5.3 设置 SettingsPage

| 设置项 | 默认值 |
|---|---|
| 客户端名称 | 空 |
| 自动重连 | 开，最多 10 次 |
| 默认分辨率 | 1920×1080 @32bpp |
| RemoteFX / GFX / H264 | 开 |
| TCP keepalive | 开，15s |
| 心跳间隔 | 30s |
| 调试级别 | WARN |

（Preferences store `settings`）

### 5.4 会话页 SessionPage

- 从书签 / 快速连接进入即发起连接；路由参数：`bookmark`（书签对象）、`displayMode`（`DISPLAY_MODE_FIT(0)`=自适应留黑边，`DISPLAY_MODE_FILL(1)`=铺满，缺省 FILL）
- 连接进度 / 状态 / 错误显示在顶栏状态文本

### 5.5 关于 AboutPage

版本信息页。

---

## 6. 会话操作

### 6.1 顶栏

| 按钮 | 功能 |
|---|---|
| ←（返回） | 断开会话并返回 |
| 状态文本 | 连接进度 / 状态 / 错误 |
| 键盘图标 | 拉起 / 收起系统输入法（IME 桥，见 6.4） |
| ⋯（more） | 特殊键面板（与系统输入法互斥，见 6.5） |

### 6.2 悬浮方向按钮

- 竖屏时显示"转横屏"图标（landscape_screen.svg），横屏时显示"转竖屏"图标（portrait_orientation.svg）
- 点击切换：竖屏 → 锁定横屏；横屏 → 恢复竖屏
- 进入会话页自动切 `AUTO_ROTATION`；离开会话页恢复进入前方向（`teardownOrientation`）

### 6.3 触控手势

| 操作 | 判定 | 远端效果 |
|---|---|---|
| 单指单击 | 位移 ≤8vp 且按压 <250ms | 左键单击 |
| 单指双击 | 两次单击间隔 ≤350ms 且相距 ≤40vp | 左键双击 |
| 长按拖动 | 按住 ≥250ms 后移动 | 左键按住拖动（拖窗口 / 框选文本） |
| 快速滑动 | 长按判定成立前快速移出阈值 | 滚轮：每 20vp 一档（120 单位），内容跟手；横向滑动发水平滚轮 |
| 双指轻点 | 第二指 ≤120ms 内落下，双指抬起间隔 ≤300ms 且位移 ≤20vp | 右键单击（抬起坐标） |
| 双指平移 | 双指位移 ≥5vp | 本地视口平移（不影响远端） |
| 双指捏合 | 捏合激活距离 ≥9vp | 视口缩放 0.3~3.0，以捏合中心为基准 |

- 互斥规则：远端拖动（已发按下）进行中，本地双指手势不接管、不改变视口
- 视口平移 / 缩放仅在画面大于视口时有意义；`clampPan` 防越界，`setViewport` 即时生效（native 侧立即重渲染）

相关常量（SessionPage.ets）：

| 常量 | 值 | 含义 |
|---|---|---|
| TAP_SLOP_VP | 8 | 单击位移阈值 |
| HOLD_DELAY_MS | 250 | 长按判定时长 |
| DOUBLE_TAP_MS | 350 | 双击间隔阈值 |
| DOUBLE_TAP_DIST_VP | 40 | 双击距离阈值 |
| TWO_FINGER_TAP_MS | 120 | 第二指落下窗口 |
| TWO_FINGER_LIFT_MS | 300 | 双指抬起间隔阈值 |
| TWO_FINGER_TAP_SLOP_VP | 20 | 右键位移阈值 |
| SCROLL_WHEEL_STEP_VP | 20 | 滚轮每档滑动距离 |
| WHEEL_DELTA_UNITS | 120 | 每档滚轮单位数 |
| MIN_SCALE / MAX_SCALE | 0.3 / 3.0 | 视口缩放范围 |

### 6.4 中文输入（IME 桥）

机制（SessionPage 隐藏输入框 `imeBridge`，1×1 透明、不参与命中测试）：

- 拼音组合过程仅在本地跟踪（`previewText`），**选字上屏**才逐码点发远端（`sendUnicodeKeyEvent`）
- 退格 = 转发 `VK_BACKSPACE(0x08)`
- **删除远端已有内容**：当一次删除会把桥接记录删空时，`onWillDelete` 否决该删除并手动补发对应数量退格 → 输入框永不清空，之后每按一次退格 = 远端删一个字符，**可无限删除 PC 端预输入内容**
- 输入超过 512 字符触发软清空（全选，等待整体替换）
- 远端光标为 I 型（IBEAM）时自动拉起输入法（native 光标位图识别，衬线像素占比 >50% 判定）
- 顶栏键盘按钮是可靠的手动开关（不依赖光标启发式）

约束：`imeBridge` 必须保持**非受控**（不得绑定 text 参数——受控模式下框架会静默取消输入法组合，详见 SessionPage 代码注释）。

### 6.5 特殊键面板（VirtualKeyboard）

Esc、F1–F12、Home/End/PgUp/PgDn/Ins/Del、方向键、Ctrl/Alt/Shift/Win（可锁定）、PrtSc/Pause/NumLock/Scroll Lock。
面板上的退格直接发远端键码，不经 IME 桥。

### 6.6 远端光标同步

- 10 种光标类型映射（`CURSOR_TYPE_*`，见 LibFreeRDP.ets）：UNKNOWN / DEFAULT / HAND / IBEAM / SIZE_NS / SIZE_WE / SIZE_NWSE / SIZE_NESW / CROSS / WAIT
- 自绘光标叠加层：跟随触控点、热点对齐、随视口缩放

### 6.7 横竖屏与桌面分辨率

- 应用全局竖屏（module.json5 `orientation: "portrait"`）；进入会话页后转 `AUTO_ROTATION`
- FILL 模式按连接发起时的屏幕快照请求桌面分辨率；旋转后由 `onAreaChange` / `onNegotiatedSettings` / `onConnectionSuccess` 触发 `maybeRequestDesktopResize()` → `freerdpRequestDesktopResize()`
- 请求带校验：生效确认，未生效自动重试（有上限）
- **需远端允许调整桌面分辨率**；服务器不允许时保持等比 letterbox 显示

---

## 7. 日志与调试

### 7.1 日志标签

| 层 | TAG |
|---|---|
| C（hilog） | `FreeRDP`（主）、`FreeRDP.HarmonyOS`、`FreeRDP.Event`、`FreeRDP.NAPI`、`FreeRDP.Clipboard`、`FreeRDP.Compat` —— 前缀筛选 `FreeRDP` 全覆盖 |
| ArkTS（console.*） | hilog tag `JSAPP` |
| Ability | `EntryAbility` 等 |

### 7.2 常用命令（PowerShell）

```powershell
# 清缓冲
hdc -t 2UCUT24C03006381 shell hilog -r

# 实时查看（会持续刷屏，Ctrl+C 结束）
hdc -t 2UCUT24C03006381 shell hilog

# 后台落盘（推荐：设备 hilog 环形缓冲会被其他应用刷掉，落盘可完整保留）
$p = Start-Process -FilePath "hdc" -ArgumentList "-t","2UCUT24C03006381","shell","hilog" `
  -RedirectStandardOutput "C:\Users\USER2022\AppData\Local\Temp\1\deveco\rdplog_live.txt" -WindowStyle Hidden -PassThru
$p.Id        # 记下 PID；测试结束后 Stop-Process -Id <pid>（文件约 33MB/小时，勿长期挂机）

# 筛选（对落盘文件）
Select-String -Path "C:\Users\USER2022\AppData\Local\Temp\1\deveco\rdplog_live.txt" -Pattern "FreeRDP|JSAPP"
```

### 7.3 关键日志速查（JSAPP / SessionPage）

| 日志 | 含义 |
|---|---|
| `touch end: tap` / `drag(N moves)` / `scroll(N wheels)` / `two-finger` / `cancelled` | 触控结束归类 |
| `scroll begin` / scroll end | 滑动滚轮起止 |
| `two-finger down (Nms), right click pending` | 双指落下，右键挂起 |
| `two-finger tap -> right click (lift Xms, move Yvp)` | 右键确认发送 |
| `two-finger not a tap` | 双指抬起超时 / 位移过大，不发右键 |
| `requesting desktop resize to WxH` | 请求远端调整桌面分辨率 |
| `ime onChange len= codes=[..] preview(len=,off)='..'` | 输入法文本变化 |
| `ime diff -> del=N ins=[..]` | 差分结果转发远端 |
| `ime preview breaks compose chain -> commit` | 组合链断裂，强制提交 |
| `ime veto emptying delete, sent N remote backspace(s)` | 删空否决 + 补发退格（删 PC 端内容的核心路径） |
| `ime backspace on empty bridge -> remote backspace` | 空桥退格直发远端 |
| `ime veto ignored by framework, skip diff` | 否决失效兜底（正常不出现） |
| `ime onWillInsert / onDidInsert / onDidDelete / onEditChange` | 输入法事件插桩（快捷短语等异常分析用） |
| `keyboard height N -> imeVisible=` | 输入法高度 / 可见性变化 |

### 7.4 原生库异常

- 页面显示原生库加载错误 → 看 `getLoadError()` 输出
- 重建原生库：见《REBUILD_NATIVE_OHOS.md》与 `scripts/rebuild-ohos-deps.ps1`（OpenSSL/zlib/cJSON）、`scripts/rebuild-ohos-native.ps1`、`scripts/rebuild-ohos-native-3.31.ps1`（FreeRDP + wrapper → `entry/libs/arm64-v8a`）

---

## 8. 已知边界与注意事项

1. IME 桥非受控约束（见 6.4），任何改动不得给 `imeBridge` 绑定 text 参数
2. 全新空字段直接按退格：依赖 `onWillDelete` 在空字段触发（真机实测可用；换输入法后异常请看 `ime onWillDelete` 日志）
3. 512 软清空后的"选中删除"存在既有 desync（罕见路径：软清空后桥接记录为空但字段有内容，此时选中删除不转发）
4. 快捷短语等输入法扩展路径已有插桩（onWillInsert / onDidInsert），异常时拉日志分析
5. 头文件更新：`entry/libs/include` **整体替换**，不逐个合并；构建参数**绝不**传 `WITHOUT_FREERDP_3x_3x_DEPRECATED`
6. `.ps1` 脚本必须保存为 **UTF-8 BOM**，否则中文注释会导致解析错误
7. 水平滚轮方向如需翻转：改 `sendAccumulatedWheel()` 的 HWHEEL 分支
8. 遗留未引用文件（勿当作现行实现参考）：`SessionView.ets`、`TouchPointerView.ets`、`KeyboardMapper.ets`、`ClipboardManager.ets`
9. `entry/build-profile.json5` cppFlags 中的 `-DWITH_OPENSSL -DWITH_FFMPEG` 为历史遗留（OpenSSL 已静态链入、FFmpeg 已禁用，CMake 注释有说明），不影响现状

---

## 9. 常见问题

| 现象 | 处理 |
|---|---|
| build_project / devecocli 报 Node 版本错误 | 用 4.1 直连 hvigorw.bat 命令 |
| install 失败（签名相关） | 见 4.3；参考根目录签名系列文档 |
| 连接失败 | 确认同一网段 / 3389 开放 / 凭据正确；看 `freerdpGetLastErrorString()` 与 FreeRDP 日志 |
| 关键日志行丢失 | 用 7.2 后台落盘方式采集 |
| 输入法不弹出 | 顶栏键盘按钮手动拉起；或确认远端光标为 I 型 |
| 旋转后画面不满屏 | 远端需允许桌面 resize（见 6.7），检查服务器策略 / 会话配置 |
| 原生库加载失败 | 见 7.4 重建原生库 |

---

## 10. 相关文档

- 《迁移指南.md》：本工程 HAR SDK 化迁移
- 《REBUILD_NATIVE_OHOS.md》：原生库重建
- 《FreeRDP-3.31-光标同步与文件打开重定向.md》：光标同步回归用例
- 根目录签名系列文档：签名配置与排障
