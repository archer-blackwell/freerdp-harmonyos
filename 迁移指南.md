# FreeRDP HarmonyOS → HAR SDK 迁移指南

> 目标：把本工程的 RDP 会话能力迁移到你现有的 SDK 工程，打包为 **HAR（静态共享包）**，供宿主应用集成。
> 本文仅为执行指南（迁移本身另行执行，不在本工程内操作）。文件、依赖、配置均对应当前源码现状核对。

---

## 1. 迁移范围

### 1.1 迁入 HAR

| 现文件（entry/src/main/ 下） | HAR 内位置（建议） | 说明 |
|---|---|---|
| ets/services/LibFreeRDP.ets | src/main/ets/sdk/ | 核心 NAPI 封装（全静态类 + 常量 + 事件接口），SDK 对外主入口 |
| ets/pages/SessionPage.ets | src/main/ets/ui/ | 会话页（需处理 @Entry/路由，见 5.2） |
| ets/components/VirtualKeyboard.ets | src/main/ets/ui/ | 特殊键面板 |
| ets/model/SessionState.ets | src/main/ets/sdk/ | ConnectionState / SessionState / GlobalSessionManager（SessionPage 依赖） |
| ets/model/BookmarkBase.ets | src/main/ets/sdk/ | 书签模型（SessionPage 路由参数类型） |
| ets/services/NetworkManager.ets | src/main/ets/sdk/ | SessionPage 依赖（SessionInfo） |
| ets/services/RdpBackgroundService.ets | src/main/ets/sdk/ | 后台保活（SessionPage 依赖） |
| cpp/（7 个源文件 + CMakeLists.txt） | src/main/cpp/ | NAPI wrapper（方案 B 时迁入） |
| libs/arm64-v8a/（4 组 .so，共 12 个文件） | libs/arm64-v8a/ | 预编译 FreeRDP 三件套 + wrapper（方案 A 时迁入） |
| libs/include/freerdp3/ + winpr3/ | （仓内随管，不入发布包） | 编译期头文件 |
| resources/rawfile/ 2 个 SVG | src/main/resources/rawfile/ | 会话页方向按钮图标 |
| resources/base/element/ 中被引用项 | src/main/resources/base/element/ | 加前缀（见 5.4） |

### 1.2 留在宿主应用

- `EntryAbility` / `EntryBackupAbility`
- `RdpSessionManager.ets` + `AudioFocusManager.ets`（EntryAbility 初始化链，与宿主生命周期强耦合；是否收编进 SDK 在 Phase 1 决策，宿主需要时可参考现 EntryAbility.ets 自行接线）
- 权限声明（4 项）、backgroundModes、签名配置、应用图标（startIcon / foreground / background / layered_image）
- 应用壳页面：Index / BookmarkPage / SettingsPage / AboutPage（SDK 形态下宿主自建 UI；如需示例页可随迁，路由问题同 5.2）

### 1.3 不迁移（当前未被引用的遗留文件）

- `components/SessionView.ets`、`components/TouchPointerView.ets`、`utils/KeyboardMapper.ets`、`utils/ClipboardManager.ets`

### 1.4 敏感信息清理（迁移前必做）

- `pages/Index.ets` 的 `buildDefaultTestBookmarks()` 写死了测试服务器（172.16.42.207）账号与明文密码 → SDK 化时移除或参数化
- 内网地址 / 测试凭据不得带入任何发布物

---

## 2. 现状依赖关系（拆包依据，来自实际 import）

```
EntryAbility ──> RdpSessionManager ──> AudioFocusManager
      └────────> NetworkManager
SessionPage ──> LibFreeRDP（连接 / 手势 / 键盘 / 视口）
      ├────────> VirtualKeyboard ──> LibFreeRDP
      ├────────> NetworkManager（SessionInfo）
      ├────────> RdpBackgroundService
      ├────────> SessionState / GlobalSessionManager
      └────────> BookmarkBase
Index ──> BookmarkBase、LibFreeRDP（原生库自检）
BookmarkPage ──> BookmarkBase
LibFreeRDP ──> import freerdpNativeImport from 'libfreerdp_harmonyos.so'（LibFreeRDP.ets:10）
```

---

## 3. HAR 模块骨架

### 3.1 目录结构（目标）

```
<SDK工程>/
  freerdp_sdk/                          # HAR 模块（名称可自定）
    oh-package.json5
    build-profile.json5
    Index.ets                           # 对外导出入口
    libs/arm64-v8a/*.so                 # 方案 A：预编译库
    src/main/module.json5               # type: "har"
    src/main/ets/sdk/...  ui/...
    src/main/cpp/...                    # 方案 B：源码编译
    src/main/resources/...
```

### 3.2 配置文件

根 `build-profile.json5` 注册模块：

```json5
"modules": [
  // ...既有模块...
  {
    "name": "freerdp_sdk",
    "srcPath": "./freerdp_sdk",
    "targets": [
      { "name": "default", "applyToProducts": ["default"] }
    ]
  }
]
```

`freerdp_sdk/oh-package.json5`：

```json5
{
  "name": "freerdp_sdk",
  "version": "1.0.0",
  "description": "FreeRDP HarmonyOS SDK (RDP client)",
  "main": "Index.ets",
  "license": "Apache-2.0",
  "dependencies": {}
}
```

`freerdp_sdk/src/main/module.json5`：

```json5
{
  "module": {
    "name": "freerdp_sdk",
    "type": "har",
    "deviceTypes": ["phone", "tablet"]
  }
}
```

`freerdp_sdk/build-profile.json5`（方案 B 含 native 配置见 4.2；方案 A 去掉 `externalNativeOptions`）：

```json5
{
  "apiType": "stageMode",
  "buildOption": {
    "externalNativeOptions": {
      "path": "./src/main/cpp/CMakeLists.txt",
      "cppFlags": "-O2 -DOHOS_PLATFORM",
      "abiFilters": ["arm64-v8a"]
    }
  },
  "buildOptionSet": [
    {
      "name": "release",
      "arkOptions": {
        "obfuscation": { "ruleOptions": { "enable": false } }
      }
    }
  ],
  "targets": [ { "name": "default" } ]
}
```

宿主依赖（本地联调）：

```json5
"dependencies": { "freerdp_sdk": "file:../freerdp_sdk" }
```

对外发布走 `ohpm publish`；包名 / 版本 / license 规范由你的 SDK 工程统一约定。

---

## 4. Native 层迁移

现状：CMake 编译 `libfreerdp_harmonyos.so`（7 个源文件），链接预编译 `libfreerdp-client3 / freerdp3 / winpr3`；OpenSSL 已静态链入这三件套，FFmpeg 构建期禁用——**无额外动态依赖**。全部产物与依赖在 `entry/libs/arm64-v8a/`（12 个文件 = 4 库 × `.so` / `.so.3` / `.so.3.31.1`）。

### 4.1 方案 A（推荐）：预编译 .so 随 HAR 分发

- 把 12 个文件全部拷入 `freerdp_sdk/libs/arm64-v8a/`（Windows 检出时即真实文件，非 symlink）
- **soname 链必须完整**：动态链接器按 `libfreerdp3.so.3` 等 SONAME 找文件，只放 `.so` 不够；首次打包后解包 HAP（后缀改 zip）检查 `libs/arm64-v8a/` 是否齐全
- 原理：HAR 的 libs 会合并进消费方 HAP，安装后进入应用 lib 目录；`XComponent({ libraryname: 'freerdp_harmonyos' })` 与 `import ... from 'libfreerdp_harmonyos.so'` 均不受影响
- 优点：消费方无需 NDK 工具链；缺点：wrapper 改动需离线重编（用 4.2 的工具链重编后替换文件）
- 验证：`LibFreeRDP.isNativeLoaded()` 为 true，`getVersion()` 返回 3.31.x

### 4.2 方案 B：HAR 内保留 CMake 源码编译

- `entry/src/main/cpp/` → `freerdp_sdk/src/main/cpp/`；头文件 → `freerdp_sdk/libs/include/`
- CMakeLists 相对路径**无需修改**：`FREERDP_DIR = ${CMAKE_CURRENT_SOURCE_DIR}/../../../libs`（src/main/cpp 向上 3 级到模块根）——只要保持 `libs/` 在模块根即可
- ⚠ 先做最小验证：你的 SDK 工程 hvigor 版本对 HAR 模块 `externalNativeOptions` 的支持情况，以实际编译结果为准；不支持则回退方案 A
- 重建依赖 / FreeRDP 的完整流程见《REBUILD_NATIVE_OHOS.md》与 `scripts/rebuild-ohos-deps.ps1`、`rebuild-ohos-native(-3.31).ps1`、`download-libs.ps1`

### 4.3 通用红线

- 构建参数**绝不**传 `WITHOUT_FREERDP_3x_3x_DEPRECATED`
- 更新 FreeRDP 时 `libs/include` 头文件**整体替换**，不逐个合并
- `libfreerdp_harmonyos.so` 名字不可改（XComponent libraryname、ArkTS native import、native 模块注册三方耦合）
- 仅支持 `arm64-v8a`（abiFilters），宿主工程不要引入其他 ABI 的依赖混编

---

## 5. ArkTS 层改造点

### 5.1 Index.ets 导出设计

```ets
// freerdp_sdk/Index.ets
export { LibFreeRDP } from './src/main/ets/sdk/LibFreeRDP'
export { EventListener, UIEventListener, BookmarkSettings, ScreenSettings,
  AdvancedSettings, PerformanceFlags, DebugSettings, GatewaySettings } from './src/main/ets/sdk/LibFreeRDP'
export { DISPLAY_MODE_FIT, DISPLAY_MODE_FILL } from './src/main/ets/sdk/LibFreeRDP'
export { CURSOR_TYPE_IBEAM /* 及其余 CURSOR_TYPE_* / VERIFY_CERT_FLAG_* / PTR_FLAGS_* 按需列举 */ } from './src/main/ets/sdk/LibFreeRDP'
export { ConnectionState, SessionState } from './src/main/ets/sdk/SessionState'
export { BookmarkBase, ManualBookmark, QuickConnectBookmark } from './src/main/ets/sdk/BookmarkBase'
// 5.2 方案二时追加：
// export { RdpSessionView } from './src/main/ets/ui/RdpSessionView'
```

LibFreeRDP 为全静态类，完整 API 面：`newInstance / freeInstance / connect / disconnect / cancelConnection / setConnectionInfo / sendCursorEvent / sendKeyEvent / sendUnicodeKeyEvent / sendSynchronizeEvent / sendClipboardData / setViewport / setSurfaceId / releaseSurface / setDisplayMode / getDisplayMode / enterBackgroundMode / exitBackgroundMode / configureAudio / setAutoReconnect / setTcpKeepalive / sendHeartbeat / requestRefresh / requestRefreshRect / getConnectionHealth / getIdleTime / checkConnectionStatus / getFrameBuffer / getLastErrorString / getVersion / isNativeLoaded / getLoadError / hasH264Support / setEventListener / setOnSettingsChanged ...`（以 services/LibFreeRDP.ets 为准）。

### 5.2 SessionPage 的 @Entry 与路由（关键决策点）

现状：`@Entry @Component struct SessionPage` + `router.pushUrl({ url: 'pages/SessionPage', params })`，页面注册于 main_pages.json。

- **方案一（改动小）**：页面留在 HAR，宿主 `main_pages.json` 注册 HAR 内页面。注册格式随 DevEco / hvigor 版本演进，**以所用版本文档为准，Phase 4 第一验证项**；失败即切方案二
- **方案二（SDK 推荐形态）**：去掉 `@Entry`，导出 `@Component struct RdpSessionView`；`SessionPageParams`（bookmark / displayMode）改为 `@Prop` 入参，宿主页面内嵌。方向管理（`setPreferredOrientation`）与 `getContext` 逻辑不用改
- 无论哪种：会话页依赖窗口 `AUTO_ROTATION`（全局竖屏下由会话页运行时切换），宿主窗口策略需允许

### 5.3 上下文与存储

- `getContext(this) as common.UIAbilityContext` 在 HAR 内取的是宿主上下文，preferences 读写无需改动
- `RdpBackgroundService` 通过 `backgroundTaskManager` 申请 continuous task 并发通知 → 依赖宿主声明 `KEEP_BACKGROUND_RUNNING` 权限与 backgroundModes（见 5.5）

### 5.4 资源

- `$rawfile('portrait_orientation.svg' / 'landscape_screen.svg')` 随 HAR 迁移后，HAR 内代码引用不用改
- element 资源建议统一加前缀（如 `frdp_`）避免与宿主同名冲突；本项目资源量小，建议执行
- 应用图标类资源不迁移，归宿主

### 5.5 权限与能力声明（HAR 不支持声明，宿主承接）

宿主 module.json5 必须包含（对照现 entry/src/main/module.json5）：

```json5
"requestPermissions": [
  { "name": "ohos.permission.INTERNET", "reason": "$string:permission_internet_reason", ... },
  { "name": "ohos.permission.GET_NETWORK_INFO", "reason": "$string:permission_network_info_reason", ... },
  { "name": "ohos.permission.KEEP_BACKGROUND_RUNNING", "reason": "$string:permission_background_reason", ... },
  { "name": "ohos.permission.RUNNING_LOCK", "reason": "$string:permission_running_lock_reason", ... }
]
```

宿主 Ability 配置 `backgroundModes: ["audioPlayback", "dataTransfer"]`；对应 reason 字符串资源随迁。

### 5.6 兼容性约束

- **API 12**：现工程 targetSdk / compatibleSdk = 5.0.0(12)，SDK 工程需 API ≥ 12。`onWillDelete / onWillInsert / onDidInsert / onDidDelete`（IME 桥核心机制）为 API 12 接口，且有"仅系统输入法"限制（真机实测可用）
- **useNormalizedOHMUrl: true**（现根 build-profile.json5）：SDK 工程该开关不一致时，HAR 资源 / 模块 URL 编译行为可能存在差异，Phase 1 统一
- **IME 桥非受控约束**：任何情况下不得给 `imeBridge` 绑定 text 参数（受控模式会静默取消输入法组合，历史教训已写入 SessionPage 注释）
- 剪贴板 / 音频 / 自动重连均由 native 回调驱动，迁移后行为不变

### 5.7 宿主初始化链（参考）

EntryAbility.onCreate → 初始化 RdpSessionManager + 会话状态回调；onForeground / onBackground 转发给管理器。SDK 若不收编 RdpSessionManager，宿主需自行处理前后台（当前 SessionPage 主链路不依赖它）。

---

## 6. 分阶段执行计划

| 阶段 | 内容 | 完成判据 |
|---|---|---|
| Phase 0 | 源工程基线固化：当前 git HEAD 5368df9 且存在大量未提交改动 → 先 commit / tag 固化"可用状态" | 可随时回退 |
| Phase 1 | SDK 工程 新建 freerdp_sdk HAR 骨架（3.2），注册模块，空 Index.ets 编译通过；统一 useNormalizedOHMUrl | hvigor 构建通过 |
| Phase 2 | 方案 A 拷贝 12 个 .so 进 libs/arm64-v8a；宿主 demo 调 `isNativeLoaded()` / `getVersion()` | 加载成功、版本正确 |
| Phase 3 | 迁 LibFreeRDP.ets（含 native import）；demo 组装 BookmarkSettings 发起连接 | 连接成功、出画面 |
| Phase 4 | 迁 VirtualKeyboard + SessionPage（按 5.2 决策）；先验证 HAR 页面注册 / 组件嵌入 | 手势六项 + IME 四项通过 |
| Phase 5 | 迁 SessionState / BookmarkBase / NetworkManager / RdpBackgroundService；宿主补权限与 backgroundModes | 后台保活 / 自动重连可用 |
| Phase 6 | 清理：删 1.3 遗留文件、1.4 敏感信息；资源前缀化；跑第 7 节全清单 | 全清单通过 |

---

## 7. 迁移验证清单

- [ ] 原生加载：`isNativeLoaded()` = true；`getVersion()` = 3.31.x
- [ ] 连接：书签 / 参数连接测试服务器 → FILL 铺满出画面
- [ ] 手势：单击 / 双击 / 长按拖动 / 滑动滚轮（纵横）/ 双指右键 / 捏合缩放 / 双指平移
- [ ] IME：拼音选字上屏、英文、删除本会话输入、退格删 PC 端已有内容、快捷短语
- [ ] 特殊键面板：Esc / F 键 / 修饰键锁定 / 方向键
- [ ] 光标：类型同步；文本框 I 型光标自动拉起输入法
- [ ] 横竖屏：会话页自动旋转 + 悬浮按钮切换 + 旋转后桌面分辨率跟随
- [ ] 后台：切后台音频不断、连接保持；回前台正常
- [ ] 剪贴板：双向文本同步
- [ ] 日志：`FreeRDP*` / `JSAPP` 标签可见（语义速查见《操作文档.md》7.3）

---

## 8. 风险与开放问题

1. HAR 内 @Entry 页面的宿主注册格式 → Phase 4 首验，失败即切 5.2 方案二（组件化导出）
2. HAR + `externalNativeOptions`（方案 B）支持度 → 先最小验证，失败回退方案 A（预编译分发）
3. HAR 静态拷贝语义：.so 与代码会复制进每个消费宿主（包体随宿主数线性增长）；未来若需多应用共享单份 so，评估 HSP（动态共享包）形态
4. Node v18 遮蔽问题若在 SDK 工程同样存在 → 构建继续直连 hvigorw.bat（命令见《操作文档.md》4.1）
5. 输入法兼容性：`onWillDelete` 等接口依赖系统输入法回调，不同输入法 / 设备需在目标环境回归
6. ohpm 发布：包名 / 版本 / license / 仓库规范按你的 SDK 工程约定执行

---

## 9. 参考资料

- 《操作文档.md》：构建 / 部署 / 日志 / 手势 / IME 现行行为
- 《REBUILD_NATIVE_OHOS.md》+ scripts/*.ps1：原生链重建
- 《FreeRDP-3.31-光标同步与文件打开重定向.md》：光标同步回归用例
- 源码：`entry/src/main/ets/services/LibFreeRDP.ets`（API 面）、`entry/src/main/ets/pages/SessionPage.ets`（会话行为）、`entry/src/main/cpp/CMakeLists.txt`（native 构建）
